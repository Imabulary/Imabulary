get:
  tags:
    - Feedback
  summary: Get all feedback categories
  responses:
    '200':
      description: A list of feedback categories
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schemas/feedback/categories.yaml'
