    get:
      tags:
        - flashcards
      summary: Get all flashcards
      description: Get a list of all flashcards
      parameters:
        - name: pagination
          in: query
          description: Pagination parameters
          schema:
            $ref: "../../components/schemas/filter/server-pagination.yaml"
        - name: filters
          in: query
          description: Filters for querying flashcards
          schema:
            $ref: "../../components/schemas/filter/filters.yaml"
      responses:
        '200':
          description: List of all flashcards
          content:
            application/json:
              schema:
                $ref: '../../components/schemas/card/all-cards-response.yaml'
      security:
        - bearerAuth: []