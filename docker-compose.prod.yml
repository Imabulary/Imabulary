version: '3'
services:
  # TEMPORARELY DISABLE AS WE'RE NOT FOCUSED ON THE WEB AT THE MOMENT
  # frontend:
  #   build:
  #     context: ./client
  #     args:
  #       NODE_ENV: production
  #   environment:
  #     - NODE_ENV=production
  #   # command: production command here

  backend:
    build:
      context: ./server
      args:
        NODE_ENV: production
    environment:
      - NODE_ENV=production
    command: npm run start:prod
